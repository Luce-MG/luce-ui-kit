import { TooltipProps as Props } from '@material-ui/core/Tooltip';
import * as React from 'react';
export declare type StyledTooltip = {
    label: string;
};
export declare type TooltipProps = Props;
declare const Tooltip: React.FC<TooltipProps>;
export default Tooltip;
