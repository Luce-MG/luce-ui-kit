import { AppBarProps as Props } from '@material-ui/core/AppBar';
import * as React from 'react';
export declare type AppBarProps = {
    children: React.ReactNode;
} & Props;
declare const AppBar: React.FC<AppBarProps>;
export default AppBar;
